<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <variable
            name="clickListener"
            type="android.view.View.OnClickListener" />

        <variable
            name="statuses"
            type="com.open.weibo.bean.Statuses" />

        <import type="com.open.weibo.utils.HBindingAdapter" />
    </data>

    <LinearLayout
        android:id="@+id/footer"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingStart="10dp"
        android:paddingEnd="10dp">

        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:clickable="true"
            android:focusable="true"
            android:gravity="center"
            android:onClick="@{clickListener::onClick}"
            android:orientation="horizontal"
            app:stateBackground="@{null}">

            <ImageView
                android:id="@+id/icon_share"
                android:layout_width="16dp"
                android:layout_height="16dp"
                app:themeImageViewTint="@{@drawable/ic_share}"
                tools:src="@drawable/ic_share" />

            <TextView
                android:id="@+id/share_count"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:gravity="center"
                android:paddingStart="5dp"
                android:paddingTop="10dp"
                android:paddingEnd="5dp"
                android:paddingBottom="10dp"
                android:text="@{HBindingAdapter.countFormat(statuses.reposts_count)}"
                app:themeTextView="@{null}" />

        </LinearLayout>

        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:clickable="true"
            android:focusable="true"
            android:gravity="center"
            android:onClick="@{clickListener::onClick}"
            android:orientation="horizontal"
            app:stateBackground="@{null}">

            <ImageView
                android:id="@+id/icon_comment"
                android:layout_width="16dp"
                android:layout_height="16dp"
                app:themeImageViewTint="@{@drawable/ic_comment}"
                tools:src="@drawable/ic_comment" />

            <TextView
                android:id="@+id/comment_count"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:gravity="center"
                android:paddingStart="5dp"
                android:paddingTop="10dp"
                android:paddingEnd="5dp"
                android:paddingBottom="10dp"
                android:text="@{HBindingAdapter.countFormat(statuses.comments_count)}"
                app:themeTextView="@{null}" />

        </LinearLayout>

        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:clickable="true"
            android:focusable="true"
            android:gravity="center"
            android:onClick="@{clickListener::onClick}"
            android:orientation="horizontal"
            app:stateBackground="@{null}">

            <ImageView
                android:id="@+id/icon_like"
                android:layout_width="16dp"
                android:layout_height="16dp"
                app:isFavorite="@{statuses.favorited}"
                app:themeImageViewTint="@{@drawable/ic_like_false}"
                tools:src="@drawable/ic_like_false" />

            <TextView
                android:id="@+id/like_count"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:gravity="center"
                android:paddingStart="5dp"
                android:paddingTop="10dp"
                android:paddingEnd="5dp"
                android:paddingBottom="10dp"
                android:text="@{HBindingAdapter.countFormat(statuses.attitudes_count)}"
                app:isFavorite="@{statuses.favorited}"
                app:themeTextView="@{null}" />
        </LinearLayout>

    </LinearLayout>

</layout>