<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:bind="http://schemas.android.com/tools">

    <data>

        <variable
            name="clickListener"
            type="android.view.View.OnClickListener" />

        <variable
            name="userProfile"
            type="com.open.weibo.bean.User" />

        <variable
            name="vm"
            type="com.open.weibo.vm.StatusesUploadViewModel" />
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical">

        <include
            android:id="@+id/top_actionbar"
            layout="@layout/layout_action_bar_header"
            app:layout_constraintBottom_toTopOf="@id/statuses_upload_et"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            bind:clickListener="@{clickListener}"
            bind:userProfile="@{userProfile}" />

        <EditText
            android:id="@+id/statuses_upload_et"
            themeEditText="@{null}"
            backgroundTint="@{null}"
            android:layout_width="match_parent"
            android:textSize="14sp"
            android:layout_height="wrap_content"
            android:autofillHints="@string/statuses_upload_hint"
            android:background="@android:color/transparent"
            android:hint="@string/statuses_upload_hint"
            android:inputType="textMultiLine"
            android:maxHeight="200dp"
            android:padding="10dp"
            android:text="@={vm.statusesStringLiveData}"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@id/top_actionbar" />

        <HorizontalScrollView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:clipChildren="false"
            android:clipToPadding="false"
            android:paddingStart="10dp"
            android:paddingTop="5dp"
            android:paddingEnd="10dp"
            android:paddingBottom="5dp"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent">

            <LinearLayout
                android:layout_width="wrap_content"
                android:layout_height="wrap_content">

                <ImageView
                    android:id="@+id/addPic"
                    themeImageViewTint="@{@drawable/ic_picture}"
                    android:layout_width="36dp"
                    android:layout_height="36dp"
                    android:layout_marginEnd="10dp"
                    android:onClick="@{clickListener::onClick}"
                    android:padding="6dp"
                    bind:src="@drawable/ic_picture" />

            </LinearLayout>
        </HorizontalScrollView>
    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>