<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <import type="com.open.weibo.utils.DrawableCreator" />

        <import type="android.view.View.OnClickListener" />

        <variable
            name="clickListener"
            type="OnClickListener" />

    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:context=".MainActivity">

        <com.facebook.drawee.view.SimpleDraweeView
            android:id="@+id/logo"
            android:layout_width="112dp"
            android:layout_height="96dp"
            android:layout_marginTop="56dp"
            android:tint="@color/logo_red"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:placeholderImage="@drawable/ic_weibo_logo"
            app:placeholderImageScaleType="fitXY" />

        <TextView
            android:id="@+id/notify_alert"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="17dp"
            android:text="@string/login_interface_authorization"
            android:textSize="18sp"
            android:textStyle="bold"
            app:layout_constraintLeft_toLeftOf="parent"
            app:layout_constraintRight_toRightOf="parent"
            app:layout_constraintTop_toBottomOf="@id/logo"
            app:themeTextView="@{null}" />

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/authorize_notification"
            android:layout_width="150dp"
            android:layout_height="150dp"
            android:layout_marginStart="15dp"
            android:layout_marginTop="16dp"
            android:layout_marginEnd="15dp"
            app:layout_constraintBottom_toTopOf="@id/button_area"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@id/notify_alert" />

        <LinearLayout
            android:id="@+id/button_area"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="16dp"
            android:clipChildren="false"
            android:clipToPadding="false"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@id/authorize_notification">

            <Button
                android:id="@+id/authorize_cancel"
                android:layout_width="75dp"
                android:layout_height="50dp"
                android:layout_marginEnd="5dp"
                android:onClick="@{clickListener::onClick}"
                android:text="@string/string_cancel"
                app:statefulPlaceHolder="@{@color/darkgray}"
                app:themeTextView="@{null}" />

            <Button
                android:id="@+id/authorize_ok"
                android:layout_width="75dp"
                android:layout_height="50dp"
                android:layout_marginStart="5dp"
                android:onClick="@{clickListener::onClick}"
                android:text="@string/string_ok"
                app:statefulPlaceHolder="@{@color/logo_red}"
                app:themeTextView="@{null}" />
        </LinearLayout>

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginStart="15dp"
            android:layout_marginTop="16dp"
            android:layout_marginEnd="15dp"
            android:gravity="center"
            android:text="@string/authorization_alert"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@id/button_area"
            app:themeSecondaryTextView="@{null}" />

    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>